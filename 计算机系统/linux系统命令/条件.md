
## test, [,  [[

  Bash测试命令, test命令或功能等价的Bash内置命令`[ ]`可以做条件测试，如果测试的结果为True，则退出状态码为0。

  此外，还可以使用[[]]来做条件测试，它比test多一个正则匹配的功能。此外，let、$[]、$(())也可以做条件测试。


  在`[[]]`中，使用=~符号开启正则测试功能，它使用的是扩展正则。
  
  =~左边是待匹配的字符串或变量，右边是正则表达式。





