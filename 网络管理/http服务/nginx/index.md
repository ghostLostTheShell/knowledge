#

## http 配置

```conf
http {
    server {
        listen 80; //配置监听端口和id 0.0.0.0:80
        server_name 0.0.0.0; //用于虚拟主机的配置

        location / { //配置监听的位置

        }

        lication / {

        }
    }
}
```