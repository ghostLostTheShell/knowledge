# 查看软件安装信息

## dpkg 命令

* dpkg --listfiles  ${程序名称}  # 查询系统中属于 ${程序名称} 的文件

* dpkg-query -L  ${程序名称}  # 查询系统中属于 ${程序名称} 的文件

* dpkg -s ${程序名称} #查看软件 ${程序名称} 的详细信息

* dpkg -l #查看系统中软件包状态

* $ dpkg-query -S ${文件名} #查看某个文件的归属包:

## pacma

* pacman -Si ${包名称} # 显示软件包的详尽的信息：