
## 合并某个分支的指定文件到当前分支

```bash
git checkout $(当前分支)

git checkout --patch $(某个分支) $(指定文件.file)
```
1. --patch

  的作用可以接受或者拒绝补丁内容

  如果没有就是简单的将文件复制到当前分支

## Git合并某个分支的一个commit到当前分支

```bash
git checkout $(当前分支)
git cherry-pick  $(commit_sha)
```