# 字节对齐问题

对齐跟数据在内存中的位置有关。如果一个变量的内存地址正好位于它长度的整数倍，他就被称做自然对齐。比如在32位cpu下，假设一个整型变量的地址为0x00000004，那它就是自然对齐的。


## 字节对齐的作用

1. 便于cpu快速访问

2. 合理的利用字节对齐可以有效地节省存储空间

## 字节不对齐引发的问题

1. 某些系统会发生 segment error

2. 影响读取效率


