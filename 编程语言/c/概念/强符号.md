强符号：函数名、初始化的全局变量名

## 转换为弱符号

1. 使用 GNU C 扩展

int a __attribute__((weak)) = 1;

## weak-alias

* 是一个宏，其目的是为函数添加一个”弱”别名，与”强”符号函数名区分。
* 说明, 如果调用函数无对应的函数无”强”符号对应的函数，则会调用该别名对应的函数
* C/C++ 函数调用是以编译后的”符号”做索引调用,详情了解 <符号表>

* 普通声明定义的函数对应的函数名是， 强”符号函数名