# 

``` ts
/**
 * T 为 string 的扩展，并且默认为 string 
 * **/
class GetMin<T extends string = string>{
    arr:T[];
    constructor (arg?:T[]){
        this.arr = arg || []; // 使用传入初始数组 或 空数组
    }
    add(num:T):T[]{  
		this.arr.push(num) // 向数组添加元素
		return this.arr;
	}
}
```