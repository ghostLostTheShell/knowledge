#

##
如果事件绑定仅写函数名称，默认会传入一个event对象，如果写带参的函数需要写入 `$event` 明确指出 fun($event, ...)
event详细查看 HTML DOM Event 对象
```vue
<template>
  <div class="pc">
    <h1>{{ msg }}</h1>
    <div
      class="demo"
      @mouseover="mouseOver"
      @mouseleave="mouseLeave"
      :style="active"
    >
      <p ref="acp">我是内容</p>
    </div>
  </div>
</template>


```