#

## 概念

1.所有的 class 声明都是一个type

``` python
class a:
  pass

isinstance(a, type) # 为 true

isinstance(a(), type) # 为flase

isinstance(object, type) # 为 true
```

## 为一个对象动态添加方法
``` python
import types

动态方法 = types.MethodType(${fun},${object})
```



## 查询系统信息
psutil