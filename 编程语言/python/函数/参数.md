# 函数参数

python 中函数的实参是引用传递

> 引用传递和引用传递
>值传递：
>
>方法调用时，实际参数把它的值传递给对应的形式参数，函数接收的是原始值的一个copy，此时内存中存在两个相等的基本类型，即实际参数和形式参数,后面方法中的操作都是对形参这个值的修改，不影响实际参数的值。
>
>引用传递：
>
>也称为传地址。方法调用时，实际参数的引用(地址，而不是参数的值)被传递给方法中相对应的形式参数，函数接收的是原始值的内存地址；
在方法执行中，形参和实参内容相同，指向同一块内存地址，方法执行中对引用的操作将会影响到实际对象。

## 参数类型

## 位置参数

## 关键字参数

**kw是关键字参数，kw接收的是一个dict

>声明只接受关键字
>def s(*, a,b,c): 声明 a,b,c 是关键字参数
>
>def s(p, *, a,b,c): 声明 p为 位置参数， a,b,c 是关键字参数
 


def ff(**, a,b,c):

## 可变参数

*args是可变参数，args接收的是一个tuple;



## __code__ 属性

可以获取函数的信息

* co_name

    获取函数的名称

* co_argcount

    获取函数的参数个数，参数个数不包含*args与**kwargs

* co_kwonlyargcount

    返回函数强制关键字参数个数，也就是*args后面的参数个数。

* co_cellvars
  
    一个元组里面存储着所有被嵌套函数用到的变量名。

* co_freevars

    一个元组里面存储着所有被函数使用的在闭包作用域中定义的变量名

* co_consts

* co_firstlineno

    代码对象的第一行位于所在文件的行号。

* co_varnames
    当前作用域定义的变量


## 特殊参数 / ,*

```txt
def f(pos1, pos2, /, pos_or_kwd, *, kwd1, kwd2):
      -----------    ----------     ----------
        |             |                  |
        |        Positional or keyword   |
        |                                - Keyword only
         -- Positional only
```