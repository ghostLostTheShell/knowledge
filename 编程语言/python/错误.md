## VS Code Pylint 出现 E0611:No name 'Qt' in module 'PyQt5.QtCore'解决办法

[env]: ["vs code" "python", "Pylint"]

[解决方法]: 在配置文件添加以下内容

```json
"python.linting.pylintArgs": [
        "--extension-pkg-whitelist=PyQt5"
    ]
```

[原因]:
新版 pylint 默认不支持外部扩展模块,pyqt5 是 C++写的外部扩展，因此在【用户设置】那里添加下面设置即可：
