---
title: Schema 到标准模式
description: 生成标准模式 V1。
sidebar:
  label: 标准模式
  order: 14
---

`Schema.standardSchemaV1` API 允许您从 Effect `Schema` 生成一个[标准模式 v1](https://standardschema.dev/) 对象。

**示例**（生成标准模式 V1）

```ts twoslash
import { Schema } from "effect"

const schema = Schema.Struct({
  name: Schema.String
})

// 将 Effect 模式转换为标准模式 V1 对象
//
//      ┌─── StandardSchemaV1<{ readonly name: string; }>
//      ▼
const standardSchema = Schema.standardSchemaV1(schema)
```

<Aside type="note" title="模式限制">
  只有没有依赖项（即 `R = never`）的模式才能转换为标准模式 V1 对象。
</Aside>

## 同步验证与异步验证

`Schema.standardSchemaV1` API 创建的模式，其 `validate` 方法会尝试同步解码和验证提供的输入。如果底层 `Schema` 包含任何异步组件（例如，异步消息解析或检查），则验证将必然返回一个 `Promise`。

**示例**（处理同步和异步验证）

```ts twoslash
import { Effect, Schema } from "effect"

// 用于显示同步和异步结果的实用函数
const print = <T>(t: T) =>
  t instanceof Promise
    ? t.then((x) => console.log("Promise", JSON.stringify(x, null, 2)))
    : console.log("Value", JSON.stringify(t, null, 2))

// 定义一个同步模式
const sync = Schema.Struct({
  name: Schema.String
})

// 生成标准模式 V1 对象
const syncStandardSchema = Schema.standardSchemaV1(sync)

// 同步验证
print(syncStandardSchema["~standard"].validate({ name: null }))
/*
输出:
{
  "issues": [
    {
      "path": [
        "name"
      ],
      "message": "Expected string, actual null"
    }
  ]
}
*/

// 定义一个带有转换的异步模式
const async = Schema.transformOrFail(
  sync,
  Schema.Struct({
    name: Schema.NonEmptyString
  }),
  {
    // 模拟异步验证延迟
    decode: (x) => Effect.sleep("100 millis").pipe(Effect.as(x)),
    encode: Effect.succeed
  }
)

// 生成标准模式 V1 对象
const asyncStandardSchema = Schema.standardSchemaV1(async)

// 异步验证
print(asyncStandardSchema["~standard"].validate({ name: "" }))
/*
输出:
Promise {
  "issues": [
    {
      "path": [
        "name"
      ],
      "message": "Expected a non empty string, actual \"\""
    }
  ]
}
*/
```

## 缺陷

如果在验证过程中发生意外缺陷，它会报告为一个没有 `path` 的单独问题。这确保了意外错误不会中断模式验证，但仍然会被捕获和报告。

**示例**（处理缺陷）

```ts twoslash
import { Effect, Schema } from "effect"

// 在解码函数中定义一个带有缺陷的模式
const defect = Schema.transformOrFail(Schema.String, Schema.String, {
  // 模拟内部失败
  decode: () => Effect.die("Boom!"),
  encode: Effect.succeed
})

// 生成标准模式 V1 对象
const defectStandardSchema = Schema.standardSchemaV1(defect)

// 验证输入，触发缺陷
console.log(defectStandardSchema["~standard"].validate("a"))
/*
输出:
{ issues: [ { message: 'Error: Boom!' } ] }
*/
```